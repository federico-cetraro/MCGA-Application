@model IEnumerable<MCGA.Data.Cancelacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.detalle_cancel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_cancel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoCancelacion.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Turno.Observaciones)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.detalle_cancel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_cancel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoCancelacion.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Turno.Observaciones)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
