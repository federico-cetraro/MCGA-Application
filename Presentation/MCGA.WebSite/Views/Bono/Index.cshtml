@model IEnumerable<MCGA.Data.Bono>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha_compra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_impresion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.consumido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Afiliado.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Plan.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_compra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_impresion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.consumido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Afiliado.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Plan.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
